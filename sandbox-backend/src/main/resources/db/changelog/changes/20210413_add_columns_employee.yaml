databaseChangeLog:
  - changeSet:
      id: 20210413_add_columns_employee
      author: skaldin
      preConditions:
        onFail: CONTINUE
        tableExists:
          tableName: EMPLOYEE
      changes:
        - addColumn:
            tableName: EMPLOYEE
            columns:
              name: EMP_LOCATION_COUNTRY
              type: VARCHAR(255)
              constraints:
                nullable: true
        - addColumn:
            tableName: EMPLOYEE
            columns:
              name: EMP_LOCATION_CITY
              type: VARCHAR(255)
              constraints:
                nullable: true
        - addColumn:
            tableName: EMPLOYEE
            columns:
              name: EMP_TIMEZONE
              type: VARCHAR(255)
              constraints:
                nullable: true
        - addColumn:
            tableName: EMPLOYEE
            columns:
              name: EMP_PASSWORD
              type: VARCHAR(255)
              constraints:
                nullable: true